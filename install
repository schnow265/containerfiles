#!/bin/bash

echo "installing, please wait."

DEBIAN_FRONTEND=noninteractive sudo apt update && sudo apt install build-essential -y

chmod +x langs/setup.sh
chmod +x shell/setup.sh

# Language Setup
./langs/setup.sh

# Shell Setup
./shell/setup.sh
