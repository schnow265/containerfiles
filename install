#!/bin/bash

echo "installing, please wait."

DEBIAN_FRONTEND=noninteractive sudo apt update && apt install lsd -y

chmod +x shell/*.sh

./shell/zsh.sh

cargo binstall starship --locked

cp config/starship.toml ~/.config/starship.toml